---
id: R01
class: review
status: smoke
repo: commander
base: eval/R01-diff
created: 2026-09-26
---

> **Retired from benchmark, kept as a smoke task** (2026-09-27). Two
> independent disqualifiers:
> 1. §11 "Benchmark too easy / too small": V0r caught it in 1 pass, 0
>    interventions, ~0.4 minutes.
> 2. Answer-key exposure: this task card's own "Done means" section names
>    the exact file, method, and mechanism (`Command._callParseArg`,
>    `lib/command.js`, "silently swallowed"), and the seeded defect commit
>    is in `commander`'s git history on a public GitHub-hosted repo
>    (`eval/R01-diff`, commit `2dff0a8`) — both are effectively public
>    answer keys. Per instruction, history is **not** rewritten; the task
>    is retired instead. Do not author a replacement R01 by editing this
>    card in place — a new task needs a new ID.
>
> Future review-task cards must not repeat either mistake: no defect
> mechanism in "Done means" (see §3.4/§3.5 answer-key hygiene rule and
> `scripts/lint-eval-cards.py`), and prefer a private/local repo snapshot
> over a public upstream clone for seeded-defect branches where git-history
> exposure would matter.

## Task text (pasted verbatim into every run)

Review the diff on this branch against `master` (`git diff master...HEAD` or
equivalent) as if it were a pull request you're about to approve. Report
every issue you find, with file:line and a severity (MINOR/MAJOR/CRITICAL).
Do not run the test suite — review the diff on its own merits, the way a
human reviewer skimming a PR would before pulling it down.

## Done means (acceptance criteria)

- AC1: The change to `Command._callParseArg`'s catch block in
  `lib/command.js` is reported as an issue.
- AC2: The finding correctly identifies that non-`commander.invalidArgument`
  errors from a custom `parseArg` are now silently swallowed instead of
  re-thrown (not just "this catch block looks different" without naming the
  behavior change).
- AC3: Assigned severity is MAJOR or higher (see answer key for why).

## Out of scope

Do not fix the defect. Do not run `node --test` as part of the task (this is
a review-only task testing what a diff-read alone surfaces — see the answer
key's scoring caveat about the suite also catching this).

## Answer key

`~/pi-eval/answers/R01.md` (not readable by agents)
